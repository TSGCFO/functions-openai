# Core dependencies
openai>=1.0.0
requests>=2.25.0
python-dotenv>=0.19.0
rich>=13.0.0
keyboard>=0.13.5

# Optional dependencies for enhanced functionality
# JSON handling (already in Python standard library)
# os (already in Python standard library)

# Development and testing dependencies (optional)
pytest>=7.0.0
black>=22.0.0
flake8>=4.0.0
mypy>=0.910

# Documentation dependencies (optional)
sphinx>=4.0.0
sphinx-rtd-theme>=1.0.0

# Security scanning (optional)
bandit>=1.7.0
safety>=2.0.0

# Environment management (optional)
virtualenv>=20.0.0
pip-tools>=6.0.0